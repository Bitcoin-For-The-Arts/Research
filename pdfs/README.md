# PDFs

Downloadable, print-ready PDF briefs for every **Bitcoin for the Arts Research** report.

The standard we hold ourselves to is the American Cancer Society's *Cancer Facts & Figures* — a single, well-designed, deeply-cited PDF that a foundation officer, journalist, or academic can download, print, mark up, and place on a desk.

## Current PDFs (v1)

| Report | File | Pages |
|---|---|---|
| The State of Arts Funding 2026 | [`state-of-arts-funding-2026.pdf`](state-of-arts-funding-2026.pdf) | 6 |
| The ARPA Cliff | [`the-arpa-cliff.pdf`](the-arpa-cliff.pdf) | 7 |
| Sound Money for the Arts | [`sound-money-for-the-arts.pdf`](sound-money-for-the-arts.pdf) | 7 |

Every figure in every PDF is derived from the canonical chart-data JSON at [`../data/reports/`](../data/reports/). Every reference in the *References* block resolves to a primary source URL. The institutional byline *Bitcoin for the Arts Research* and the "last reviewed" date appear in the cover block of each brief.

## What's in each brief

The v1 PDF brief is in the Brookings / Pew / ACS register — typography-driven, dense, citable, footnote-rigorous. Each brief contains:

1. **Cover** — kicker, title, abstract, byline block (byline / last reviewed / license / suggested citation), and the report's four headline statistics ("hero strip"), each numbered against the *References* block.
2. **Abstract** — repeated on the body's first page for journalists and researchers who skim.
3. **"Why this brief exists" callout** — the BFTA Research credibility frame in one paragraph.
4. **Report structure** — the section list of the live, web-rendered version of the report.
5. **Headline charts** — every chart from the JSON, rendered as a clean SVG bar chart with a source caption beneath each one.
6. **"Where to read the full report"** — link to the canonical web version and the source JSON in this repository.
7. **References** — numbered, with permanent URLs to every primary source.
8. **Colophon** — provenance, corrections workflow, license, and the 501(c)(3) registration.

The full narrative text of each report lives at [bitcoinforthearts.org/research](https://bitcoinforthearts.org/research). These PDFs are the citable, print-ready briefs that sit alongside the web version.

## Generation

PDFs are generated by [`../scripts/build-pdfs.mjs`](../scripts/build-pdfs.mjs):

```bash
npm install                          # one-time
npm run validate:data                # check every source-id reference resolves
npm run build:pdfs                   # build every report
node scripts/build-pdfs.mjs the-arpa-cliff   # build one
```

The build script:

1. Reads `data/reports/<slug>.json` (the canonical, citable source of truth).
2. Renders the report through an inline HTML/CSS template (Brookings/ACS register, cream background, black text on Liberation Serif, orange/lime accents, full citation apparatus).
3. Generates an SVG bar chart inline for every chart in the JSON. Numbers come from the data file; no hardcoded values.
4. Pipes the HTML through headless Chrome (`puppeteer-core` + the system Chrome at `CHROME_PATH`) to produce a print-ready PDF.

Set the `CHROME_PATH` environment variable if your Chrome / Chromium binary is somewhere other than `/usr/local/bin/google-chrome`.

### Fonts

The template uses the open Liberation Serif family for body text and Liberation Sans for accents, with fallbacks to Tinos, DejaVu, Noto, Georgia, and system stacks. No font is downloaded at build time; the build runs against whatever the system has installed. Liberation Serif and Liberation Sans ship with most Linux distributions.

## Versioning

PDFs follow the report versioning scheme set out in [`../METHODOLOGY.md`](../METHODOLOGY.md) — `v1`, `v1.1`, `v2`, and so on. When the underlying JSON dataset changes, the PDF is regenerated and replaced; the prior PDF is preserved in git history.

The "last reviewed" date in the PDF cover and the running header is the date a human reviewer last verified every citation. The PDF's last-reviewed date always matches the JSON dataset's `lastReviewed` field.

## Citing a PDF

> Bitcoin for the Arts Research. (2026). *[Report title]* (PDF). Bitcoin For The Arts, Inc. https://bitcoinforthearts.org/research/[slug]

Where a specific page or figure is being cited, append the page or chart ID. The chart IDs match the `charts[].id` field in the report JSON, so a citation like "*The ARPA Cliff*, chart `nea-arp-distribution`" is unambiguous and machine-resolvable.

## Roadmap

v1 is the **structurally correct** brief: every figure cited, every chart rendered from the dataset, every reference resolved. v1.1 → v1.x will iterate on typography density and chart polish toward the ACS *Cancer Facts & Figures* target — without compromising the citation discipline that makes the briefs worth citing in the first place.

Future passes:

- **v1.x** — typographic polish (small-caps, drop-caps, refined chart axis treatment, hero photo on cover)
- **v2** — full narrative body of each report set into the brief (currently the brief is structure + headline charts + references; v2 adds the full prose of the live web report, source-linked inline)
- **v3** — multi-format build (PDF, EPUB, accessible HTML, executive one-pager) from the same JSON + Markdown source
